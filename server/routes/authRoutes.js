const express = require('express');
const router = express.Router();

router.post('/register', () => {});
router.post('/login', () => {});
router.get('/login', () => {});
router.get('/register', () => {});
